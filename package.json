{
  "scripts": {
    "build": "node ./build.js",
    "watch": "watch \"npm run build\" ./src",
    "serve": "http-server ./dist -p 8080",
    "local": "concurrently \"npm run watch\" \"npm run serve\""
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "http-server": "^0.12.3",
    "watch": "^1.0.2"
  },
  "dependencies": {
    "fs-extra": "^9.0.1",
    "node-sass": "^4.14.1"
  }
}
